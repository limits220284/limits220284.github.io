import{j as l,b as a,c as e,aa as i}from"./chunks/framework.Bxf40uRC.js";const k=JSON.parse('{"title":"TCP","description":"","frontmatter":{},"headers":[],"relativePath":"计算机网络/TCP.md","filePath":"计算机网络/TCP.md","lastUpdated":null}'),t={name:"计算机网络/TCP.md"},s=i(`<h1 id="tcp" tabindex="-1">TCP <a class="header-anchor" href="#tcp" aria-label="Permalink to &quot;TCP&quot;">​</a></h1><h2 id="重传机制" tabindex="-1">重传机制 <a class="header-anchor" href="#重传机制" aria-label="Permalink to &quot;重传机制&quot;">​</a></h2><h3 id="_1-超时重传" tabindex="-1">1.超时重传 <a class="header-anchor" href="#_1-超时重传" aria-label="Permalink to &quot;1.超时重传&quot;">​</a></h3><p><strong>RTT</strong>：Round-Trip Time，往返时延</p><p><strong>RTO</strong>：Retransmission Timeout 超时重传时间</p><p>超时触发重传存在的问题是，超时周期可能相对较长。那是不是可以有更快的方式呢？</p><h3 id="_2-快速重传" tabindex="-1">2. 快速重传 <a class="header-anchor" href="#_2-快速重传" aria-label="Permalink to &quot;2. 快速重传&quot;">​</a></h3><p><strong>不以时间为驱动，而是以数据驱动重传</strong>。</p><p>假如发送的是数据1-6，其中2丢失了，服务端在接收到三个同样的ACK时候，就会触发重发机制</p><p>快速重传机制只解决了一个问题，就是超时时间的问题，但是它依然面临着另外一个问题。就是<strong>重传的时候，是重传一个，还是重传所有的问题。</strong> 比如2，3都丢了，但是接收到4，5，6的时候会返回三次ACK 2，发送方无法确定重传的个数是多少；</p><h3 id="_3-sack-方法" tabindex="-1">3. SACK 方法 <a class="header-anchor" href="#_3-sack-方法" aria-label="Permalink to &quot;3. SACK 方法&quot;">​</a></h3><p><strong>SACK</strong>(selective acknowledgment)，选择性确认</p><ol><li><p>需要在头部加入一个SACK的东西，它可以将已经收到的数据的信息发送给发送方，这样发送方就可以知道哪些数据收到了，哪些数据没有收到，知道了这些信息，就可以只重传丢失的数据；</p><p><img src="https://s2.loli.net/2024/04/10/gMT8vY2l6EOaXe9.png" alt="image-20240410145641775"></p></li></ol><p>相当于接收方会通过SACK返回收到部分数据的信息；</p><p>但是这样也会存在一些问题：</p><ol><li>如果是因为网络原因造成的发送的信息很久未到，可以让发送端知道原因；</li><li>如果是因为ACK丢包了，也可以告诉发送端，不是因为自己的原因，自己发送的已经到达了；</li></ol><p><strong>Duplicate SACK</strong> 又称 <code>D-SACK</code>，其主要<strong>使用了 SACK 来告诉「发送方」有哪些数据被重复接收了。</strong></p><p>用来解决上面的两个问题:</p><ol><li>如果是因为ACK报文丢失了，下次发送方再次发送该报文的时候，接收方会在SACK报文中携带这段重复接受数据的信息；</li></ol><h2 id="滑动窗口" tabindex="-1">滑动窗口 <a class="header-anchor" href="#滑动窗口" aria-label="Permalink to &quot;滑动窗口&quot;">​</a></h2><blockquote><p>存在原因：传统的TCP都是发送完毕数据之后，进行一个ACK，再发送下一个，这样效率很低；</p></blockquote><p>进而TCP引入了<strong>窗口</strong>这个概念，有了窗口，可以指定窗口大小，<strong>无需等待确认应答，而可以继续发送数据的最大值</strong>；</p><p><img src="https://s2.loli.net/2024/04/10/HukTviaMQwbezUl.png" alt="image-20240410151018749"></p><p>就算某个时刻，ack = 600丢失了，发送方也可以通过ACK = 700来确定之前的数据都被接收到了；称之为<strong>累计应答</strong>；</p><blockquote><p>窗口大小由哪一方决定？</p></blockquote><p>TCP头部存在一个字段<strong>Window</strong>，这个字段是<strong>接收端告诉发送端</strong>还有多少缓冲区可以接收数据。于是发送端就可以根据这个接收端的处理能力来发送数据，而不会导致接收端处理不过来；</p><p>所以通常是由接收方窗口大小来决定的；</p><blockquote><p>发送方如何表示窗口？</p></blockquote><ol><li>SND.WND：表示发送窗口的大小；</li><li>SND.UNA： 是一个绝对指针，指向的是已经发送但是未收到确认的第一个字节的序列号；</li><li>SND.NXT：是一个绝对指针，指向未发送但是可以发送的第一个字节的序列号；</li></ol><p><img src="https://s2.loli.net/2024/04/10/qkZCegH2L9Xba5x.png" alt="image-20240410153133451"></p><blockquote><p>接收方如何表示窗口？</p></blockquote><p><img src="https://s2.loli.net/2024/04/10/h9NdyYzgXfJ8uCq.png" alt="image-20240410153319103"></p><p>RCV.WND：表示接收窗口的大小</p><p>RCV.NXT：表示期望从发送方发送来的下一个数据字节的序列号</p><p>未收到并且不可以接受的数据序列号就可以通过RCV.NXT + RCV.WND计算出来;</p><h2 id="流量控制" tabindex="-1">流量控制 <a class="header-anchor" href="#流量控制" aria-label="Permalink to &quot;流量控制&quot;">​</a></h2><p><strong>存在原因</strong>：发送方不能无脑的发送数据给接收方，要考虑接收方的处理能力，如果一直无脑的发数据给对方，但是对方处理不过来，就会导致触发重发机制，从而导致网络流量的无端浪费；</p><blockquote><p>怎么做的？</p></blockquote><ol><li>发送方窗口大小，控制发送的频率和大小</li><li>通过操作系统的缓冲区大小，来动态维护窗口大小；</li></ol><h2 id="拥塞控制" tabindex="-1">拥塞控制 <a class="header-anchor" href="#拥塞控制" aria-label="Permalink to &quot;拥塞控制&quot;">​</a></h2><p><strong>存在原因</strong>：主要为了解决网络堵塞问题，流量控制只是为了解决发送/接收双方的信息处理能力；</p><p><strong>在网络出现拥堵时，如果继续发送大量数据包，可能会导致数据包时延、丢失等，这时 TCP 就会重传数据，但是一重传就会导致网络的负担更重，于是会导致更大的延迟以及更多的丢包，这个情况就会进入恶性循环被不断地放大....</strong></p><blockquote><p>拥塞窗口？和发送窗口什么关系？</p></blockquote><p><strong>拥塞窗口cwnd</strong>是发送方维护的一个状态变量，它会根据<strong>网络的拥塞程度</strong>动态变化；</p><p>swnd(发送窗口) = min(cwnd(拥塞窗口)，rwnd(接收窗口))</p><p>如果没有发生堵塞，cwnd就会增大，否则减小</p><blockquote><p>如何得知网络是否出现了堵塞？</p></blockquote><p>很简单，只要发送方没有在规定时间内接收到ACK应答报文，就是发生了超时重传，就会认为网络出现了拥塞！</p><blockquote><p>拥塞控制存在哪些算法?</p></blockquote><ol><li>慢启动</li><li>拥塞避免</li><li>拥塞发生</li><li>快速恢复</li></ol><h3 id="慢启动" tabindex="-1">慢启动 <a class="header-anchor" href="#慢启动" aria-label="Permalink to &quot;慢启动&quot;">​</a></h3><p>规则：当发送方每收到一个ACK，拥塞窗口cwnd的大小就会加1；</p><ul><li>连接建立完成后，一开始初始化 <code>cwnd = 1</code>，表示可以传一个 <code>MSS</code> 大小的数据。</li><li>当收到一个 ACK 确认应答后，cwnd 增加 1，于是一次能够发送 2 个</li><li>当收到 2 个的 ACK 确认应答后， cwnd 增加 2，于是就可以比之前多发2 个，所以这一次能够发送 4 个</li><li>当这 4 个的 ACK 确认到来的时候，每个确认 cwnd 增加 1， 4 个确认 cwnd 增加 4，于是就可以比之前多发 4 个，所以这一次能够发送 8 个。</li></ul><p><img src="https://s2.loli.net/2024/04/10/uUKdlxH1EyjQATa.png" alt="image-20240410155357514"></p><blockquote><p>难道可以一直增加吗？</p></blockquote><p>参数ssthresh(slow start threshold) 状态变量</p><ul><li>当 cwnd &lt; ssthresh：慢启动</li><li>cwnd &gt;= ssthrest：拥塞避免算法</li></ul><h3 id="拥塞避免算法" tabindex="-1">拥塞避免算法 <a class="header-anchor" href="#拥塞避免算法" aria-label="Permalink to &quot;拥塞避免算法&quot;">​</a></h3><p>规则：每当收到一个ack后，cwnd增加 $\\frac{1}{cwnd} $</p><p>接上前面的慢启动的栗子，现假定 <code>ssthresh</code> 为 <code>8</code>：</p><ul><li>当 8 个 ACK 应答确认到来时，每个确认增加 1/8，8 个 ACK 确认 cwnd 一共增加 1，于是这一次能够发送 9 个 <code>MSS</code> 大小的数据，变成了<strong>线性增长。</strong></li></ul><p><img src="https://s2.loli.net/2024/04/10/GJ5UFHCMZklPv9y.png" alt="image-20240410155812791"></p><p>但是这样还会出现一直增长的情况，于是就会出现丢包情况，这时就需要对丢掉的数据包进行重传，</p><p>当触发了重传机制，也就进入了拥塞发生算法;</p><h3 id="拥塞发生算法" tabindex="-1">拥塞发生算法 <a class="header-anchor" href="#拥塞发生算法" aria-label="Permalink to &quot;拥塞发生算法&quot;">​</a></h3><p>存在原因：拥塞避免算法随着时间增长，数据包过多，导致的网络堵塞，造成了重传机制；</p><ul><li>超时重传</li><li>快速重传</li></ul><blockquote><p>发生<strong>超时重传</strong>的拥塞发生算法</p></blockquote><ul><li>ssthresh设为cwnd/2</li><li>cwnd重置为1(恢复初始值)</li></ul><blockquote><p>怎么查看系统的cwnd初始化值？</p></blockquote><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ss</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -nli</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cwnd</span></span></code></pre></div><p><img src="https://s2.loli.net/2024/04/10/smORuXHjqSFicnw.png" alt="image-20240410170515573"></p><p>但是这样问题在于，之前的发送大小一般很大，但是突然恢复到初始值，比较激进，容易造成卡顿；</p><blockquote><p>发生<strong>快速重传</strong>的拥塞发生算法</p></blockquote><p>还有更好的方式，前面我们讲过「快速重传算法」。当接收方发现丢了一个中间包的时候，发送三次前一个包的 ACK，于是发送端就会快速地重传，不必等待超时再重传。</p><p>TCP 认为这种情况不严重，因为大部分没丢，只丢了一小部分，则 <code>ssthresh</code> 和 <code>cwnd</code> 变化如下：</p><ul><li><code>cwnd = cwnd/2</code> ，也就是设置为原来的一半;</li><li><code>ssthresh = cwnd</code>;</li><li>进入快速恢复算法</li></ul><h3 id="快速恢复算法" tabindex="-1">快速恢复算法 <a class="header-anchor" href="#快速恢复算法" aria-label="Permalink to &quot;快速恢复算法&quot;">​</a></h3><p>一般和快速重传算法结合使用，在进入快速恢复之前，cwnd和ssthresh已经被更新了</p><ul><li><code>cwnd = cwnd/2</code> ，也就是设置为原来的一半;</li><li><code>ssthresh = cwnd</code>;</li></ul><p>步骤：</p><ol><li>拥塞窗口 cwnd = ssthresh + x(代表有三个数据包被收到了)； <ul><li>相当于失去的三个数据包都收到了，然后处于拥塞避免状态，那么就直接+ 1/cwnd即可；</li></ul></li><li>重传丢失的数据包；</li><li>如果再重复收到ACK，那么cwnd + 1； <ul><li>为了快速重传丢失的包，只能增大窗口</li></ul></li><li>如果收到新数据的ACK后，把cwnd设置为第一步的ssthresh值，原因是该ACK确认了新的数据，说明从duplicated ACK时候的数据都已经收到，该恢复过程已经结束，可以回到之前的状态了，也即再次进入拥塞避免状态； <ul><li>收到新数据ACK，表示收到了新数据，此时没必要保持这种状态了，因为收到重复ACK是为了快速重发，并没有进行拥塞避免；</li></ul></li></ol><p><img src="https://s2.loli.net/2024/04/10/ROxCwiKEZNLeMuA.png" alt="image-20240410172028595"></p><p><strong>首先，快速恢复是拥塞发生后慢启动的优化，其首要目的仍然是降低 cwnd 来减缓拥塞，所以必然会出现 cwnd 从大到小的改变。</strong></p><p><strong>其次，过程2（cwnd逐渐加1）的存在是为了尽快将丢失的数据包发给目标，从而解决拥塞的根本问题（三次相同的 ACK 导致的快速重传），所以这一过程中 cwnd 反而是逐渐增大的。</strong></p><h2 id="tcp-keepalive机制的工作原理" tabindex="-1">TCP Keepalive机制的工作原理 <a class="header-anchor" href="#tcp-keepalive机制的工作原理" aria-label="Permalink to &quot;TCP Keepalive机制的工作原理&quot;">​</a></h2><ol><li><strong>启用Keepalive</strong>：在大多数操作系统中，TCP Keepalive默认可能是关闭的或者具有一个非常长的超时时间。应用程序可以通过设置socket选项来启用Keepalive，并配置相关参数，如探测间隔和探测次数。</li><li><strong>探测（Probe）发送</strong>：一旦Keepalive被启用，并且在配置的超时时间内没有在连接上检测到任何数据交换，操作系统将开始发送Keepalive探测。这些探测是特殊的TCP包，目的是检查对端是否仍然响应。</li><li><strong>响应检查</strong>：如果对端正常响应了Keepalive探测，表示连接仍然有效，操作系统将重置Keepalive计时器，并在下一个周期再次等待。如果对端没有响应，操作系统会继续发送探测，直到达到配置的探测次数限制。</li><li><strong>连接终止</strong>：如果所有Keepalive探测都没有得到响应，操作系统将认为连接已经断开，并通知应用程序。这通常通过在socket上报告一个错误来实现。</li></ol><h2 id="怎么解决tcp粘包问题" tabindex="-1">怎么解决TCP粘包问题？ <a class="header-anchor" href="#怎么解决tcp粘包问题" aria-label="Permalink to &quot;怎么解决TCP粘包问题？&quot;">​</a></h2><h3 id="_1、固定消息长度" tabindex="-1">1、固定消息长度 <a class="header-anchor" href="#_1、固定消息长度" aria-label="Permalink to &quot;1、固定消息长度&quot;">​</a></h3><p>和UDP操作方式相同，规定每个发送的消息的长度都是一致的，当接收方接收到这个长度的消息的时候，就认为这是一个有效的消息；</p><p>缺点：不够灵活，实际不常用</p><h3 id="_2、特殊字符当作边界" tabindex="-1">2、特殊字符当作边界 <a class="header-anchor" href="#_2、特殊字符当作边界" aria-label="Permalink to &quot;2、特殊字符当作边界&quot;">​</a></h3><p>可以在两个用户消息之间插入一个特殊的字符串，这样接收方在接收数据时，读到了这个字符串，就代表已经读完了一个完整的消息；</p><p>HTTP就是一个非常好的例子;</p><p><img src="https://s2.loli.net/2024/04/16/LQ1y3JwdEpPl7kx.png" alt="图片"></p><p>HTTP 通过设置回车符、换行符作为 HTTP 报文协议的边界。</p><p>有一点要注意，这个作为边界点的特殊字符，如果刚好消息内容里有这个特殊字符，我们要对这个字符转义，避免被接收方当作消息的边界点而解析到无效的数据</p><h3 id="_3、自定义消息结构" tabindex="-1">3、自定义消息结构 <a class="header-anchor" href="#_3、自定义消息结构" aria-label="Permalink to &quot;3、自定义消息结构&quot;">​</a></h3><p>我们可以自定义一个消息结构，由包头和数据组成，其中包头是固定大小的，包头里面有一个字段可以指定后续数据有多大；</p><div class="language-c++ vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c++</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    u_int32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message_length; </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    char</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message_data[]; </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} message;</span></span></code></pre></div>`,100),o=[s];function n(p,r,c,h,d,g){return a(),e("div",null,o)}const b=l(t,[["render",n]]);export{k as __pageData,b as default};
