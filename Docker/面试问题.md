# 面试问题

##  虚拟化和容器的区别？

## Docker为什么比虚拟机快？

1、Docker有着比虚拟机更少的抽象层；

2、Docker利用的是宿主机的内核，VM需要是的Guest OS；

3 、所以说，新建一个容器的时候，Docker不需要像虚拟机一样重新加载一个操作系统内核，避免引导。虚拟机施加载Guest OS是分钟级别的，而Docker是利用宿主机的操作系统的，省略了这个复杂的过程；