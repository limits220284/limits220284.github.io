# Virtual Environment

1. 创建虚拟环境

   ```python
   python3 -m venv myenv
   ```

2. 激活虚拟环境

   ```python
   source myenv/bin/activate
   ```

3. 使用虚拟环境

    ```shell
    crtl + shift + p -> python interpreter
    ```

      