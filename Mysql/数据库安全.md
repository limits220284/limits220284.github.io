# 数据库安全

## Sql注入怎么防护

1. 严格限制web应用的数据库的操作权限，给连接数据库的用户提供满足需要的最低权限，最大限度的减少注入攻击对数据库造成的危害
2. 校验参数的数据格式是否合法(可以使用正则或者特殊字符的判断)
   - SELECT * FROM users WHERE username = '' OR '1'='1' AND password = '输入的密码';
3. 对进入数据库的特殊字符进行转义处理，或编码转换
4. 预编译SQL(java中使用PreparedStatement)，参数化查询方式，避免SQL拼接

